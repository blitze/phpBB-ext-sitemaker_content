<input type="password" class="inputbox autowidth" name="{{ field_name }}" id="{{ field_id }}" size="{{ field_props.size }}" minlength="{{ field_props.min }}" maxlength="{{ field_props.max }}" value="{{ field_value }}" />