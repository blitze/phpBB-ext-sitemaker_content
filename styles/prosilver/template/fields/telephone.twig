<div style="width: {{ field_props.size|default('100') }}%">
	<input type="tel" class="inputbox autowidth" id="smc-{{ field_name }}" name="{{ field_name }}" maxlength="{{ field_props.max }}" value="{{ field_value }}" />
</div>